#include "main.h"

/**
 * int_abs - Function to check absolute value of an integer
 * @i:
 * Return: 
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
	return (0);
}
