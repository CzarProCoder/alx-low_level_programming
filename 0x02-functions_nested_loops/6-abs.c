#include "main.h"

/**
 * int_abs - Function to check absolute value of an integer
 * @i:
 * Return: 
 */

int _abs(int)
{
	int i;
	int abs_val;

	if (i > 0)
	{
		abs_val = i
	}
	else
	{
		abs_val = (- (-i))
	}
	return (abs_val);
}
